<div class="max-w-2xl mx-auto py-8 space-y-6">
  <!-- Breadcrumb -->
  <nav class="flex items-center space-x-2 text-sm text-gray-500">
    <%= link_to @project.name, project_path(@project), class: "hover:text-gray-700" %>
    <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/></svg>
    <span class="text-gray-900 font-medium">Add to Knowledge Base</span>
  </nav>

  <!-- Header -->
  <div class="text-center">
    <h1 class="text-2xl font-bold text-gray-900">Add to Knowledge Base</h1>
    <p class="mt-2 text-gray-500">Save a note, article, link, file, or idea to your project</p>
  </div>

  <!-- Form -->
  <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
    <%= render "form", project: @project, knowledge_entry: @knowledge_entry %>
  </div>
</div>
